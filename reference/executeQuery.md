# Execute a query

The initialize query is executed and the result is returned as a
QueryResult object

## Usage

``` r
executeQuery(.Object, model)

# S4 method for class 'Query'
executeQuery(.Object, model)
```

## Arguments

- .Object:

  a Query object

- model:

  a Model object containing the statements to query

## Value

a QueryResults object
